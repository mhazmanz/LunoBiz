# LunoBiz configuration template (DO NOT put real secrets in this file)
# Copy to .env (which is git-ignored) and fill real values there.

# ===== LUNO API =====
# Create separate keys for read-only and live trading. Start with read-only.
LUNO_API_KEY_READONLY=
LUNO_API_SECRET_READONLY=

# LIVE keys (leave blank until system passes paper-trading gates)
LUNO_API_KEY_LIVE=
LUNO_API_SECRET_LIVE=

# ===== TELEGRAM (optional) =====
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ===== APP MODE =====
# PAPER or LIVE (LIVE will still be blocked unless gates pass)
APP_MODE=PAPER

# ===== MARKETS =====
# Default view pair (Malaysia example; adjust to the exact Luno pair symbol you trade)
DEFAULT_VIEW_PAIR=ETHMYR

# Comma-separated list to scan; leave empty to auto-discover from Luno
SCAN_PAIRS=

# ===== RISK SETTINGS (AGGRESSIVE TARGET: 3-5% daily is extremely high risk) =====
# Per-trade risk as fraction of equity, e.g. 0.005 = 0.5%
RISK_PER_TRADE=0.005

# Daily loss cap fraction of equity, e.g. 0.02 = 2% max loss/day before stopping
DAILY_LOSS_CAP=0.02

# Max simultaneous positions
MAX_OPEN_POSITIONS=2

# Cooldown minutes after hitting daily loss cap or consecutive loss threshold
COOLDOWN_MINUTES=240

# ===== BACKTEST SETTINGS =====
BACKTEST_TIMEFRAME=5m
BACKTEST_SLIPPAGE_BPS=8
BACKTEST_FEE_BPS=30

# ===== STORAGE =====
# Local data folder (relative path). Will be created if missing.
DATA_DIR=data
DB_FILENAME=lunobiz.sqlite3

# ===== PERFORMANCE =====
# Main loop sleep seconds (reduce to react faster, increase to reduce API usage)
POLL_INTERVAL_SECONDS=10
